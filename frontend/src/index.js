import React from "react";
import ReactDOM from "react-dom/client";
import SSOLogin from "./signon";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <SSOLogin />
  </React.StrictMode>
);
